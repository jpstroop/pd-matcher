"""Package marker for shipped stopword JSON resources.

The JSON files in this directory are loaded by
:mod:`pd_matcher.normalize.stopwords` via :mod:`importlib.resources` so they
are guaranteed to ship with installed wheels.
"""
